      - name: Debug and List Projects
        run: |
          # 这一步会打印出所有被 Gradle 识别的模块名称
          ./gradlew projects
          
          # 这一步尝试构建（假设模块名是 :android，如果报错，请看上面的输出）
          ./gradlew :android:assembleDebug --stacktrace
